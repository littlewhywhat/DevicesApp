<Grid x:Class="MiaAppInterface.DataGrid"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:y="clr-namespace:MiaAppInterface"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" DataContext="{Binding}">
    <Grid.RowDefinitions>
        <RowDefinition Height="1*"></RowDefinition>
        <RowDefinition Height="9*"></RowDefinition>
        
    </Grid.RowDefinitions>
    <StackPanel Orientation="Horizontal">
        <Button Background="#FFF9F9F9" Margin="5" Name="Update" Click="Update_Click">Update</Button>
        <Button Background="#FFF9F9F9" Margin="5" Name="Delete" Click="Delete_Click">Delete</Button>
        
    </StackPanel>
    <Grid Name="contentGrid" Grid.Row="1" DataContext="{Binding}">
    </Grid>
</Grid>
