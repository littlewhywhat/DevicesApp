<Grid x:Class="MiaAppInterface.DevicesGrid"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:y="clr-namespace:MiaAppInterface"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="500" DataContext="{Binding}">
    <Grid.ColumnDefinitions>
        <ColumnDefinition></ColumnDefinition>
        <ColumnDefinition></ColumnDefinition>
    </Grid.ColumnDefinitions>
    <y:TabItemGrid x:Name="ManagerGrid" DataContext="{Binding}">
        <y:TabItemGrid.ContentGrid>
            <y:DevicesInfoGrid></y:DevicesInfoGrid>
        </y:TabItemGrid.ContentGrid>
    </y:TabItemGrid>
    <Grid Grid.Column="1">

        <y:EventsListBox Margin="5" Grid.Column="1" DataContext="{Binding Mode=OneTime}"></y:EventsListBox>
        
    </Grid>
    
    
</Grid>
