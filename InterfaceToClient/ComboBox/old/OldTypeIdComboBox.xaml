<ComboBox x:Class="InterfaceToClient.OldTypeIdComboBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"  
             d:DesignHeight="300" d:DesignWidth="300" Template="{DynamicResource CustomComboBox}"
          DataContextChanged="ComboBox_DataContextChanged" SelectionChanged="ComboBox_SelectionChanged">
    <ComboBox.ItemTemplate>
        <DataTemplate>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding Path=Name, BindsDirectlyToSource=True}" HorizontalAlignment="Left"></TextBlock>
                <TextBlock Margin="5,0,0,0" Text="{Binding Path=Marker, BindsDirectlyToSource=True}" HorizontalAlignment="Right"></TextBlock>
            </StackPanel>
            
        </DataTemplate>

    </ComboBox.ItemTemplate>
</ComboBox>
