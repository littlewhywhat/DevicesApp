<y:DataItemsInfoGrid x:Class="InterfaceToClient.DevicesInfoGrid"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:y="clr-namespace:InterfaceToClient"
             xmlns:mwt="clr-namespace:Microsoft.Windows.Themes;assembly=PresentationFramework.Aero"
             mc:Ignorable="d" 
             d:DesignHeight="383.8" d:DesignWidth="301.6"  DataContext="{Binding}" DataContextChanged="DataItemsInfoGrid_DataContextChanged">
    <Grid.Resources>
        <Style TargetType="FrameworkElement" x:Key="Default">
            <Setter Property="Margin" Value="5"></Setter>
        </Style>
    </Grid.Resources>
    <StackPanel Style="{StaticResource Default}" >
        <y:DeviceTypesInfoGridShort DataContext="{Binding Path=Element.Type}" IsEnabled="False"></y:DeviceTypesInfoGridShort>
        
        <TextBlock>Заводской номер:</TextBlock>
        <y:EnableTextBox x:Name="DeviceProductNumber" TextChanged="DeviceProductNumber_TextChanged" Text="{Binding Path=Element.Name}"></y:EnableTextBox>
        
        <TextBlock>Компания:</TextBlock>
        <y:CompanyIdComboBox   x:Name="DevicesCompanyIdComboBox" DataContext="{Binding Path=Element}"></y:CompanyIdComboBox>
        <StackPanel Orientation="Horizontal">
            <TextBlock>Тип изделия:</TextBlock>
            <TextBlock Margin="5,0,0,0" Text="{Binding ElementName=DevicesTypeIdComboBox, Path=SelectedValue.Marker}"></TextBlock>
        </StackPanel>

        <y:TypeIdComboBox x:Name="DevicesTypeIdComboBox" DataContext="{Binding Path=Element}" SelectionChanged="DevicesTypeIdComboBox_SelectionChanged"></y:TypeIdComboBox>
        <TextBlock>Родитель:</TextBlock>
        <y:ParentComboBox x:Name="DevicesTypeParentComboBox" DataContextChanged="DevicesTypeParentComboBox_DataContextChanged" DataContext="{Binding Path=Element}" ></y:ParentComboBox>

    </StackPanel>
</y:DataItemsInfoGrid>
