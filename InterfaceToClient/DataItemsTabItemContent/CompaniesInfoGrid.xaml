<y:DataItemsInfoGrid x:Class="InterfaceToClient.CompaniesInfoGrid"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:y="clr-namespace:InterfaceToClient"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" DataContext="{Binding}" >
    <StackPanel Name="Main" DataContext="{Binding}" >
        <TextBlock>Название компании:</TextBlock>
        <y:EnableTextBox x:Name="CompanyName" Text="{Binding Path=Name}"></y:EnableTextBox>
        <TextBlock>Информация о компании:</TextBlock>
        <y:EnableTextBox x:Name="CompanyInfo" Text="{Binding Path=Info}"></y:EnableTextBox>
        <TextBlock>Родительская компания:</TextBlock>
        <y:ParentComboBox x:Name="CompanyParentComboBox" ItemsSource="{Binding Path=Parents}" SelectedValue="{Binding Path=ParentOrDefault}"></y:ParentComboBox>
    </StackPanel>
</y:DataItemsInfoGrid>
